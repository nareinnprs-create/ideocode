[package]
name = "ideocode-selfdev-types"
version = "0.1.0"
license = "MIT"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
