[package]
name = "ideocode"
version = "1.0.0"
license = "MIT"
description = "Possibly the greatest coding agent ever built — blazing-fast TUI, multi-model, swarm coordination, 30+ tools"
edition = "2024"
autobins = false

[workspace]
members = [
    ".",
    "crates/ideocode-agent-runtime",
    "crates/ideocode-app-core",
    "crates/ideocode-base",
    "crates/ideocode-tui",
    "crates/ideocode-build-meta",
    "crates/ideocode-ambient-types",
    "crates/ideocode-auth-types",
    "crates/ideocode-embedding",
    "crates/ideocode-gateway-types",
    "crates/ideocode-import-core",
    "crates/ideocode-pdf",
    "crates/ideocode-logging",
    "crates/ideocode-background-types",
    "crates/ideocode-batch-types",
    "crates/ideocode-build-support",
    "crates/ideocode-compaction-core",
    "crates/ideocode-config-types",
    "crates/ideocode-core",
    "crates/ideocode-fuzzy",
    "crates/ideocode-harness-api",
    "crates/ideocode-harness-api-server",
    "crates/ideocode-desktop2",
    "crates/ideocode-memory-types",
    "crates/ideocode-message-types",
    "crates/ideocode-overnight-core",
    "crates/ideocode-plan",
    "crates/ideocode-productivity-core",
    "crates/ideocode-swarm-core",
    "crates/ideocode-protocol",
    "crates/ideocode-selfdev-types",
    "crates/ideocode-session-types",
    "crates/ideocode-setup-hints",
    "crates/ideocode-storage",
    "crates/ideocode-side-panel-types",
    "crates/ideocode-azure-auth",
    "crates/ideocode-notify-email",
    "crates/ideocode-provider-metadata",
    "crates/ideocode-provider-env",
    "crates/ideocode-provider-core",
    "crates/ideocode-provider-bedrock",
    "crates/ideocode-provider-anthropic",
    "crates/ideocode-provider-antigravity",
    "crates/ideocode-provider-copilot",
    "crates/ideocode-provider-openrouter",
    "crates/ideocode-provider-openai",
    "crates/ideocode-provider-gemini",
    "crates/ideocode-provider-gemini-runtime",
    "crates/ideocode-provider-cursor-runtime",
    "crates/ideocode-provider-antigravity-runtime",
    "crates/ideocode-provider-copilot-runtime",
    "crates/ideocode-provider-claude-cli-runtime",
    "crates/ideocode-provider-openrouter-runtime",
    "crates/ideocode-provider-anthropic-runtime",
    "crates/ideocode-provider-openai-runtime",
    "crates/ideocode-provider-doctor",
    "crates/ideocode-tui-markdown",
    "crates/ideocode-tui-messages",
    "crates/ideocode-usage-types",
    "crates/ideocode-tui-core",
    "crates/ideocode-tui-mermaid",
    "crates/ideocode-task-types",
    "crates/ideocode-tool-core",
    "crates/ideocode-tool-types",
    "crates/ideocode-tui-account-picker",
    "crates/ideocode-tui-anim",
    "crates/ideocode-tui-render",
    "crates/ideocode-tui-visual-debug",
    "crates/ideocode-tui-session-picker",
    "crates/ideocode-tui-style",
    "crates/ideocode-tui-permissions",
    "crates/ideocode-tui-tool-display",
    "crates/ideocode-tui-usage-overlay",
    "crates/ideocode-update-core",
    "crates/ideocode-terminal-launch",
    "crates/ideocode-terminal-image",
    "crates/ideocode-telemetry-core",
    "crates/ideocode-tui-workspace",
    "crates/ideocode-desktop",
    "crates/ideocode-render-core", "crates/ideocode-provider-baanzon",
]

exclude = ["ideocode-gui"]

[lib]
name = "ideocode"
path = "src/lib.rs"

[[bin]]
name = "ideocode"
path = "src/main.rs"

[[bin]]
name = "test_api"
path = "src/bin/test_api.rs"

[[bin]]
name = "ideocode-harness"
path = "src/bin/harness.rs"

[[bin]]
name = "session_memory_bench"
path = "src/bin/session_memory_bench.rs"
required-features = ["dev-bins"]

[[bin]]
name = "memory_recall_bench"
path = "src/bin/memory_recall_bench.rs"
required-features = ["dev-bins"]

[[bin]]
name = "mermaid_side_panel_probe"
path = "src/bin/mermaid_side_panel_probe.rs"
required-features = ["dev-bins"]

[[bin]]
name = "tui_bench"
path = "src/bin/tui_bench.rs"
required-features = ["dev-bins"]

[dependencies]
# Async runtime
tokio = { version = "1", features = ["fs", "io-std", "io-util", "macros", "net", "process", "rt-multi-thread", "signal", "sync", "time"] }
futures = "0.3"
async-trait = "0.1"

# HTTP client
reqwest = { version = "0.12", default-features = false, features = ["json", "charset", "http2", "system-proxy", "rustls-tls", "rustls-tls-native-roots"] }
rustls = { version = "0.23", default-features = false, features = ["aws_lc_rs"] }
tokio-tungstenite = { version = "0.24", default-features = false, features = ["connect", "rustls-tls-native-roots"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["raw_value"] }
toml = "0.8"

# CLI
clap = { version = "4", features = ["derive"] }

# File operations

# Utilities
dirs = "5"               # home directory
anyhow = "1"
libc = "0.2"             # Unix system calls (flock)
chrono = { version = "0.4", features = ["serde"] }

# Embeddings (local inference) - behind feature flag (163 crates, slow to compile)

# OAuth
sha2 = "0.10"
hex = "0.4"
open = "5"               # Open URLs in browser
ideocode-tui-session-picker = { path = "crates/ideocode-tui-session-picker", features = ["serde"] }

# Streaming
tokio-stream = "0.1"

# TUI
ratatui = "0.30"
crossterm = { version = "0.29", features = ["event-stream"] }

# Markdown & syntax highlighting

# PDF parsing (behind feature flag)
ideocode-build-meta = { path = "crates/ideocode-build-meta" }
# Tiny leaf helpers (console/env/fs/id) used directly by the cli layer.
# Presentation layer: the terminal UI (`tui`) + offline replay (`video_export`),
# compiled as a separate rustc unit. It re-exports the application core
# (`ideocode-app-core`, which re-exports `ideocode-base`), so the root crate (cli +
# bin) re-exports everything via `pub use ideocode_tui::*`. default-features=false
# so the root feature set fully controls the downstream features (see
# [features]).
ideocode-tui = { path = "crates/ideocode-tui", default-features = false }
# `ideocode provider-doctor` / provider diagnostics; sits downstream of ideocode-base
# so doctor edits do not rebuild the base -> app-core -> tui spine.
ideocode-provider-doctor = { path = "crates/ideocode-provider-doctor" }
# Gemini provider runtime; sits downstream of ideocode-base so gemini edits do
# not rebuild the base -> app-core -> tui spine. Registered with the external
# provider registry at startup (src/cli/startup.rs).
ideocode-provider-gemini-runtime = { path = "crates/ideocode-provider-gemini-runtime" }
ideocode-provider-cursor-runtime = { path = "crates/ideocode-provider-cursor-runtime" }
ideocode-provider-antigravity-runtime = { path = "crates/ideocode-provider-antigravity-runtime" }
ideocode-provider-copilot-runtime = { path = "crates/ideocode-provider-copilot-runtime" }
ideocode-provider-claude-cli-runtime = { path = "crates/ideocode-provider-claude-cli-runtime" }
ideocode-provider-openrouter-runtime = { path = "crates/ideocode-provider-openrouter-runtime" }
ideocode-provider-anthropic-runtime = { path = "crates/ideocode-provider-anthropic-runtime" }
ideocode-provider-openai-runtime = { path = "crates/ideocode-provider-openai-runtime" }
ideocode-selfdev-types = { path = "crates/ideocode-selfdev-types" }

# Archive extraction (for auto-update)
tempfile = "3"

[features]
# Include local ONNX/tokenizer embeddings in default builds so memory recall,
# semantic retrieval, and embedding-backed features work out of the box.
# Use `IDEOCODE_DEV_FEATURE_PROFILE=minimal` for compile-speed probes that need
# to skip optional default feature stacks.
default = ["pdf", "embeddings", "bedrock"]
dev-bins = ["ideocode-tui/dev-bins"]
jemalloc = [
    "dep:tikv-jemallocator",
    "tikv-jemallocator/stats",
    "ideocode-tui/jemalloc",
]
jemalloc-prof = [
    "jemalloc",
    "tikv-jemallocator/profiling",
    "ideocode-tui/jemalloc-prof",
]
# Local embeddings and PDF extraction live in ideocode-base / ideocode-app-core;
# these features just forward down the stack.
embeddings = ["ideocode-tui/embeddings"]
# Live AWS Bedrock support (the AWS SDK stack) lives in ideocode-provider-bedrock;
# forwards down through ideocode-tui -> ideocode-app-core -> ideocode-base.
bedrock = ["ideocode-tui/bedrock"]
# Internal release feature: build a supported OpenSSL inside the CentOS 7
# compatibility image while preserving the glibc 2.17 runtime baseline.
linux-compat-vendored-openssl = ["ideocode-tui/linux-compat-vendored-openssl"]
mmdr-size-api = ["ideocode-tui/mmdr-size-api"]
pdf = ["ideocode-tui/pdf"]

[target.'cfg(not(windows))'.dependencies]
# Memory allocator (reduces fragmentation for long-running server). The
# underlying jemalloc C library cannot build on Windows MSVC: its autotools
# build requires sh + make, and upstream marks the msvc target untested. The
# jemalloc/jemalloc-prof features stay enabled on Windows (so --all-features is
# green on all platforms) but compile as no-ops that fall back to the system
# allocator there.
tikv-jemallocator = { version = "0.6", features = ["unprefixed_malloc_on_supported_platforms"], optional = true }

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = ["Win32_Foundation", "Win32_System_Threading"] }

[target.'cfg(target_os = "macos")'.dependencies]
global-hotkey = "0.7"
objc2 = "0.6"
block2 = "0.6"
objc2-foundation = { version = "0.3", features = [
    "NSString",
    "NSThread",
    "NSRunLoop",
    "NSDate",
    "NSTimer",
    "NSUserDefaults",
    "NSValue",
    "NSAttributedString",
    "NSDictionary",
    "block2",
] }
objc2-app-kit = { version = "0.3", features = [
    "NSAppearance",
    "NSApplication",
    "NSResponder",
    "NSStatusBar",
    "NSStatusItem",
    "NSStatusBarButton",
    "NSButton",
    "NSCell",
    "NSColor",
    "NSControl",
    "NSFont",
    "NSFontDescriptor",
    "NSImage",
    "NSAttributedString",
    "NSMenu",
    "NSMenuItem",
    "NSRunningApplication",
] }

[profile.release]
opt-level = 1
debug = 0
codegen-units = 256
incremental = true

[profile.selfdev]
inherits = "release"
opt-level = 0

# Full LTO release for stable/distribution builds
[profile.release-lto]
inherits = "release"
lto = "thin"
codegen-units = 16
incremental = false

[profile.dev]
debug = 0
incremental = true

[profile.dev.package."*"]
opt-level = 0

# Keep the idle-animation math kernels fully optimized even in debug-style
# builds. This crate is pure, dependency-free numeric code (the trig-heavy 3D
# samplers that otherwise dominate idle CPU). Pinning it to opt-level = 3 here
# means the optimized object is compiled once and reused across iterative
# dev/selfdev rebuilds, since it only recompiles when the animation code itself
# changes. The "*" glob above sets the default for dependencies; these explicit
# package entries take precedence for this one crate in each profile.
[profile.dev.package."ideocode-tui-anim"]
opt-level = 3

[profile.selfdev.package."ideocode-tui-anim"]
opt-level = 3

[profile.test.package."ideocode-tui-anim"]
opt-level = 3

# The fuzzy matcher runs a char-level DP over every picker entry on every
# keystroke (e.g. /model search across hundreds of routes). Like the anim
# kernels above, it is small, pure, dependency-free code that rarely changes,
# so keeping it fully optimized in dev/selfdev builds costs one compile and
# removes visible input lag in unoptimized builds.
[profile.dev.package."ideocode-fuzzy"]
opt-level = 3

[profile.selfdev.package."ideocode-fuzzy"]
opt-level = 3

[profile.test.package."ideocode-fuzzy"]
opt-level = 3

[profile.release.package."ideocode-fuzzy"]
opt-level = 3

# Keep the text-shaping stack optimized even in dev/selfdev/test builds.
#
# cosmic-text + rustybuzz + ttf-parser + swash + yazi do all of the desktop
# transcript glyph shaping. At opt-level = 0 they are 15-40x slower, which made
# scrolling real (emoji/markdown-heavy) transcripts janky in dev/selfdev builds
# even though the production release build was smooth. These are stable
# third-party crates that almost never recompile, so pinning them to opt-level =
# 3 costs a one-time compile and is then reused across every iterative ideocode
# rebuild (same rationale as ideocode-tui-anim above). It does NOT slow down
# recompiles of ideocode's own crates.
[profile.dev.package.cosmic-text]
opt-level = 3
[profile.selfdev.package.cosmic-text]
opt-level = 3
[profile.test.package.cosmic-text]
opt-level = 3

[profile.dev.package.rustybuzz]
opt-level = 3
[profile.selfdev.package.rustybuzz]
opt-level = 3
[profile.test.package.rustybuzz]
opt-level = 3

[profile.dev.package.ttf-parser]
opt-level = 3
[profile.selfdev.package.ttf-parser]
opt-level = 3
[profile.test.package.ttf-parser]
opt-level = 3

[profile.dev.package.swash]
opt-level = 3
[profile.selfdev.package.swash]
opt-level = 3
[profile.test.package.swash]
opt-level = 3

[profile.dev.package.yazi]
opt-level = 3
[profile.selfdev.package.yazi]
opt-level = 3
[profile.test.package.yazi]
opt-level = 3

[profile.dev.package.fontdb]
opt-level = 3
[profile.selfdev.package.fontdb]
opt-level = 3
[profile.test.package.fontdb]
opt-level = 3

# Keep the raster image pipeline optimized in dev/selfdev/test builds.
#
# Inline transcript images decode (png/zune-jpeg/fdeflate/miniz_oxide), resize
# (image), and base64-encode (kitty transmit) at draw time; at opt-level 0 a
# single screenshot costs 100-200ms per decode/resize, which reads as input
# lag. These are pure pixel crates with no ideocode logic, so optimizing them
# does not hurt rebuild times for our own code.
[profile.dev.package.image]
opt-level = 3
[profile.selfdev.package.image]
opt-level = 3
[profile.test.package.image]
opt-level = 3

[profile.dev.package.png]
opt-level = 3
[profile.selfdev.package.png]
opt-level = 3
[profile.test.package.png]
opt-level = 3

[profile.dev.package.zune-jpeg]
opt-level = 3
[profile.selfdev.package.zune-jpeg]
opt-level = 3
[profile.test.package.zune-jpeg]
opt-level = 3

[profile.dev.package.fdeflate]
opt-level = 3
[profile.selfdev.package.fdeflate]
opt-level = 3
[profile.test.package.fdeflate]
opt-level = 3

[profile.dev.package.miniz_oxide]
opt-level = 3
[profile.selfdev.package.miniz_oxide]
opt-level = 3
[profile.test.package.miniz_oxide]
opt-level = 3

[profile.dev.package.base64]
opt-level = 3
[profile.selfdev.package.base64]
opt-level = 3
[profile.test.package.base64]
opt-level = 3

# session_search scans multi-megabyte session files with memchr-based
# case-insensitive matching; keep the SIMD scanner optimized everywhere.
[profile.dev.package.memchr]
opt-level = 3
[profile.selfdev.package.memchr]
opt-level = 3
[profile.test.package.memchr]
opt-level = 3

# The embedding stack (tract ONNX inference + HF tokenizer) runs the MiniLM
# model for memory recall/maintenance on the shared server. At opt-level 0 a
# single embed measured ~666 ms (matmul inner loops unoptimized), which keeps
# the model "busy" long enough that the idle unloader rarely fires and burns
# CPU on every memory-maintenance pass. These crates are pure compute and
# change only when the dependency is bumped, so the optimized objects are
# compiled once and cached across dev/selfdev iterations.
[profile.dev.package.tract-core]
opt-level = 3
[profile.selfdev.package.tract-core]
opt-level = 3
[profile.test.package.tract-core]
opt-level = 3
[profile.dev.package.tract-linalg]
opt-level = 3
[profile.selfdev.package.tract-linalg]
opt-level = 3
[profile.test.package.tract-linalg]
opt-level = 3
[profile.dev.package.tract-data]
opt-level = 3
[profile.selfdev.package.tract-data]
opt-level = 3
[profile.test.package.tract-data]
opt-level = 3
[profile.dev.package.tract-hir]
opt-level = 3
[profile.selfdev.package.tract-hir]
opt-level = 3
[profile.test.package.tract-hir]
opt-level = 3
[profile.dev.package.tract-onnx]
opt-level = 3
[profile.selfdev.package.tract-onnx]
opt-level = 3
[profile.test.package.tract-onnx]
opt-level = 3
[profile.dev.package.tract-onnx-opl]
opt-level = 3
[profile.selfdev.package.tract-onnx-opl]
opt-level = 3
[profile.test.package.tract-onnx-opl]
opt-level = 3
[profile.dev.package.tract-nnef]
opt-level = 3
[profile.selfdev.package.tract-nnef]
opt-level = 3
[profile.test.package.tract-nnef]
opt-level = 3
[profile.dev.package.ndarray]
opt-level = 3
[profile.selfdev.package.ndarray]
opt-level = 3
[profile.test.package.ndarray]
opt-level = 3
[profile.dev.package.tokenizers]
opt-level = 3
[profile.selfdev.package.tokenizers]
opt-level = 3
[profile.test.package.tokenizers]
opt-level = 3

[profile.test]
debug = 0
incremental = true
codegen-units = 256

[dev-dependencies]
async-stream = "0.3"
# Enables the downstream test-support helpers (storage::lock_test_env,
# auth::test_sandbox, bus::reset_models_updated_publish_state_for_tests, the
# ExternalAuthReviewCandidate read accessors) for the root crate's own cli test
# target. Feature unification applies this only to test/bench/example builds,
# never to a normal `cargo build`/release binary.
ideocode-tui = { path = "crates/ideocode-tui", default-features = false, features = ["test-support"] }

[build-dependencies]
