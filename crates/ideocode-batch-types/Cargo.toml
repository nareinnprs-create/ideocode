[package]
name = "ideocode-batch-types"
version = "0.1.0"
license = "MIT"
edition = "2024"
publish = false

[dependencies]
ideocode-message-types = { path = "../ideocode-message-types" }
serde = { version = "1", features = ["derive"] }
