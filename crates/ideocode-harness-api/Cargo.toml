[package]
name = "ideocode-harness-api"
version = "0.1.0"
license = "MIT"
edition = "2024"
publish = false
description = "Stable, versioned client API for the ideocode harness (agent runtime)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
