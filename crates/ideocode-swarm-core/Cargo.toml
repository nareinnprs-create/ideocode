[package]
name = "ideocode-swarm-core"
version = "0.1.0"
license = "MIT"
edition = "2024"
publish = false

[dependencies]
ideocode-plan = { path = "../ideocode-plan" }
serde = { version = "1", features = ["derive"] }
